name: Swift

on: [push]

jobs:
  build:
  
    runs-on: self-hosted
    
    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: |
       xcodebuild clean -workspace "HGF Collective.xcworkspace" -scheme "HGF Collective"
       cp ~/Downloads/HGFCollective/HGF\ Collective/GoogleService-Info.plist ~/Downloads/HGFCollective/_work/HGFcollective/HGFcollective/HGF\ Collective/GoogleService-Info.plist
       xcodebuild -workspace "HGF Collective.xcworkspace" -scheme "HGF Collective"
  lint:
  
    runs-on: self-hosted
    
    needs: build
    steps:
    - name: Lint
      run: |
       swiftlint --version
       swiftlint lint --strict --config .swiftlint.yml
